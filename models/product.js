class Product {
  constructor(id, ownderId, title, imageUrl, description, price){
    this.id=id,
    this.ownderId=ownderId,
    this.title=title,
    this.imageUrl=imageUrl,
    this.description=description,
    this.price=price
  }
}

export default Product